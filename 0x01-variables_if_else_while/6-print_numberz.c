#include <stdio.h>
/**
 * main - Entry Point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int count = 48;

	while (count < 58)
	{
		putchar(count);
		count++;
	}
	putchar(10);
	return (0);
}

